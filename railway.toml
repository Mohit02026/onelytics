[build]
builder = "nixpacks"
buildCommand = "npm run build && npx playwright install --with-deps chromium"

[deploy]
startCommand = "npx prisma migrate deploy && npm start"
healthcheckPath = "/api/health"
restartPolicyType = "on_failure"
